---
import { getEntry, render } from 'astro:content';
import Layout from "../layouts/Layout.astro";

const welcome = await getEntry("posts", "welcome")!;
const { Content } = await render(welcome);
---

<Layout>
	<Content/>
</Layout>

